[project]
name = "fixedpointserver"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.0",
    "python-frontmatter>=1.1.0",
    "scikit-learn>=1.6.1",
    "watchdog>=6.0.0",
]
